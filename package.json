{
  "name": "parcel-template",
  "version": "1.0.0",
  "description": "",
  "source": [
    "./src/pages/index.html",
    "./src/pages/page.html"
  ],
  "scripts": {
    "test": "jest",
    "watch": "jest --watchAll",
    "dev": "parcel -p 3000 src/pages/*.html",
    "build": "rm -rf dist/* && parcel build src/pages/index.html --public-url ./",
    "publish": "git subtree push --prefix dist origin gh-pages",
    "clean": "rm -rf dist/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/babel-preset-env": "^2.12.0",
    "@parcel/transformer-sass": "^2.12.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-transform-stub": "^2.0.0",
    "parcel": "^2.12.0"
  }
}
